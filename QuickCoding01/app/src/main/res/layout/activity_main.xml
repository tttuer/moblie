<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.pca.quickcoding.MainActivity"
    android:onClick="onButton1Clicked">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/input"
        android:hint="값을 입력하세요."
        android:gravity="center"
        android:layout_toLeftOf="@+id/insertBtn"
        android:layout_toStartOf="@+id/insertBtn" />

    <Button
        android:id="@+id/insertBtn"
        android:text = "확인"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:onClick="onButtonInsert"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Show Minimum"
        android:id="@+id/mini"
        android:padding="30dp"
        android:layout_marginTop="64dp"
        android:layout_below="@+id/input"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:onClick="findMiniBtn" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/average"
        android:text="Show average"
        android:padding="30dp"
        android:onClick="findAverBtn"
        android:layout_alignTop="@+id/mini"
        android:layout_alignRight="@+id/insertBtn"
        android:layout_alignEnd="@+id/insertBtn" />
</RelativeLayout>
